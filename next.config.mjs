/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The marketing page is a static scroll narrative. Nothing here is dynamic yet.
  // Stripe and the licence surfaces arrive as route handlers, not as changes here.
};
export default nextConfig;
